#include <iostream>
#include <cmath>


using namespace std;


int main(){

    int wholeNum = 5;
    double decimalNum = 5.5;

    wholeNum += 90;

    cout << wholeNum << endl;
    cout << pow(2, 5) << endl;
    cout << sqrt(36.36363636) << endl;
    cout << round(4.6) << endl;
    cout << ceil(4.2) << endl;
    cout << floor(4.8) << endl;
    cout << fmin(10, 20) << endl;
    cout << fmax (10,20) << endl;

    return 0;

}
